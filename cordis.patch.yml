# dsh-reasoning-effort-inject profile patch layer: one loader entry for the
# plugin. The host half is a trivial no-op Cordis plugin so the bundle
# resolves; the browser half is picked up by dsh-client-modules through the
# package's dsh.client declaration, exactly like the shipped ui-* packages.
- insert:
    - id: reasoning-effort-inject
      name: 'dsh-reasoning-effort-inject'
